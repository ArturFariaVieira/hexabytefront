import { routeConstants } from "shared/constants";
import UpdateProduct from ".";

// eslint-disable-next-line import/no-anonymous-default-export
export default {
  path: routeConstants.UPDATEPRODUCT.route,
  component: <UpdateProduct />,
};
